<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.RingtonePicker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/light_bg</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.RingtonePicker" parent="Base.Theme.RingtonePicker" />

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
<!--        <item name="android:navigationBarColor">@color/white</item>-->
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/shape_sheet_dialog_bg</item>
    </style>


</resources>